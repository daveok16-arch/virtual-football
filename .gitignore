# Dependencies
node_modules/

# Secrets / env — NEVER commit real credentials
.env
.env.local
.env.*.local

# Derived / large captured artifacts (regenerated by the scrapers)
scheduled-virtuals.html
virtual-football-live.ndjson

# Logs
*.log

# OS / editor cruft
.DS_Store
*.swp
.idea/
.vscode/
# Persisted calibration training data (accumulated resolved matches)
calibration-data.json
